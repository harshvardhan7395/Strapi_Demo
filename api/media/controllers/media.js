'use strict';
const { sanitizeEntity ,parseMultipartData,convertRestQueryParams,finder} = require('strapi-utils');


/**
 * Read the documentation (https://strapi.io/documentation/3.0.0-beta.x/concepts/controllers.html#core-controllers)
 * to customize this controller
 */

module.exports = {
  // async create (ctx){
       /**
   * Create a record.
   *
   * @return {Object}
   */
        //console.log(ctx.state);
      // console.log("Hello");

        //const {data} = parseMultipartData(ctx);
       // const{where} = convertRestQueryParams(ctx);
        //console.log(where[9]);
         //console.log("After initialising\n");
       // console.log(obj);
        
       // console.log(convertRestQueryParams(ctx));      
    
    //},
};
